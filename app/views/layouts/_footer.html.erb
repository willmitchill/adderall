<header>


        <small>

          <% if current_user %>
            Signed in as <%= current_user.email %> (<%= link_to "Log out", session_path("current"), method: :delete %>)(<%=link_to "#{current_user.email}'s Dashboard", profile_path%>)
            (<%= link_to "Universities", universities_path %>)
          <% else %>
            <%= link_to "Log In", new_session_path %> | <%= link_to "Sign Up", new_user_path %>
          <% end %>
        </small>


  <small>

  </small>
</header>
